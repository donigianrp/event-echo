import prisma from "@/db";

export async function POST(request: Request) {
  // try {
  //     const resp = prisma.eventSeries.create({
  //         data: {
  //         }
  //     })
  // } catch {
  // }
}
